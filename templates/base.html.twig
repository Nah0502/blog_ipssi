<!DOCTYPE html>
<html>
<head>

    {% include 'header.html.twig' %}
</head>
<body>

    {% block body %}
        <style>
            .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        </style>
        
        <div class="example-wrapper">
            <h1>Les 3 dernieres categorie! ✅</h1>
        
            {% for categorie in categories %}
                <h3>{{ categorie.titre }}</h3>
                <a href="{{ path('app_categorie_show', {'id': categorie.id}) }}">Voire la categorie</a>
            {% endfor %}
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_categorie_new')}}">Ajouter une categorie</a>
                <a href="{{ path('app_article_new')}}">Ajouter un article</a>
            {% endif %}

        </div>
    {% endblock %}
</body>
</html>